@model IEnumerable<ClassInfo>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Jiu Jitsu Log</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="JiuJitsuLog" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-square"></i> &nbsp; Create New Log Entry
            </a>
        </div>
    </div>
    <br />

    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th width="16%">
                    Date
                </th>
                <th width="10%">
                    Belt Color
                </th>
                <th width="17%">
                    Class Uniform (Gi or No Gi)
                </th>
                <th width="40%">
                    Class Focus
                </th>
                <th width="7%">
                    Mat Time
                </th>
                <th width="10%"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td>
                        @obj.ClassLogDateTime
                    </td>
                    <td>
                        @obj.BeltColor
                    </td>
                    <td>
                        @obj.ClassUniform
                    </td>
                    <td>
                        @obj.ClassFocus
                    </td>
                    <td>
                        @obj.MatTime
                    </td>
                    <td>
                        <div class="btn-group" role="group">
                            <a asp-controller="JiuJitsuLog" asp-action="Edit" asp-route-id="@obj.Id"
                            class="btn btn-primary mx-3"> <i class="bi bi-pencil-square"></i>Edit</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

